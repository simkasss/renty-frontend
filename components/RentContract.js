export default function RentContract({ property, onBack }) {
    return (
        <div className="property-details">
            <div className="property-info">
                <h2>{`Rent Contract No. ${"X"}`}</h2>
                <p>{`Property NFT id: ${"X"}`}</p>
                <p>{`Tenant: ${"X"}`}</p>
                <p>{`Rental Term: ${"X"}`}</p>
                <p>{`Rental Price: ${"X"}`}</p>
                <p>{`Deposit Amount: ${"X"}`}</p>
                <p>{`Start Date: ${"X"}`}</p>
                <p>{`End Date: ${"X"}`}</p>
                <p>{`Status: ${"X"}`}</p>
                <p>{`Hash of Terms and Conditions: X`}</p>
            </div>
        </div>
    )
}
